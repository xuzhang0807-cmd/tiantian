# Network Blueprint

Models the nginx stream + sing-box structure only. This blueprint intentionally does not include real `/etc/sing-box/config.json`, private keys, UUIDs, short IDs, or generated client links.

Generate secrets on the target host and keep them outside Git.

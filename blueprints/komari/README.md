# Komari Blueprint

Models live Komari at `127.0.0.1:8083 -> 25774` with data bind `/home/komari/data` and nginx domain `komari.kazerush.xyz`.

Always back up `/home/komari/data` before removing or recreating the container.

# Sub2API Blueprint

Models `sub2api` on `127.0.0.1:8080` with PostgreSQL, Redis, app data Docker volumes, and nginx at `sub2.kazerush.xyz`.

Do not commit `.env`, generated admin credentials, JWT secret, database dumps, or Docker volume data.
